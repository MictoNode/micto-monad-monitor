# Test-only dependencies (not installed into the Docker image).
#
# Usage: pip install -r requirements-dev.txt   # pulls runtime deps too
-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
responses==0.26.3
