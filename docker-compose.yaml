services:
  monad-validator-monitor:
    image: ghcr.io/mictonode/micto-monad-monitor:latest
    # build: .  # Uncomment to build from source instead
    container_name: monad-validator-monitor
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config/config.yaml:/app/config/config.yaml:ro
      - ./config/validators.yaml:/app/config/validators.yaml:ro
      - monitor-state:/app/state
    ports:
      - "8181:8181"
      - "8282:8282"
      - "8383:8383"
    environment:
      - TZ=${TZ:-UTC}
      - PYTHONUNBUFFERED=1
      - HEALTH_PORT=${HEALTH_PORT:-8181}
      - DASHBOARD_PORT=${DASHBOARD_PORT:-8282}
      - API_PORT=${API_PORT:-8383}
      - DASHBOARD_PASSWORD=${DASHBOARD_PASSWORD:-}
      - DASHBOARD_JWT_SECRET=${DASHBOARD_JWT_SECRET:-}
      - DASHBOARD_COOKIE_SECURE=${DASHBOARD_COOKIE_SECURE:-auto}
    depends_on:
      - prometheus
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8181/health', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  prometheus:
    # Pinned to the release this deployment already runs, so an image pull can
    # never trigger an unintended TSDB upgrade. Bump deliberately.
    image: prom/prometheus:v3.14.0
    container_name: monad-prometheus
    restart: unless-stopped
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.retention.time=30d'
      # Whichever limit trips first wins: on a busy deployment this size cap can
      # shorten the 30 day window, so scale it to the prometheus-data volume if
      # you rely on long ranges. See "Prometheus retention" in the README.
      - '--storage.tsdb.retention.size=5GB'
    volumes:
      - ./config/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - monitor-state:/etc/prometheus/targets:ro
      - prometheus-data:/prometheus
    ports:
      - "9090:9090"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  monitor-state:
  prometheus-data:
