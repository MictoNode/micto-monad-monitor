global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'monad_validators'
    file_sd_configs:
      - files:
          - '/etc/prometheus/targets/validators.json'
        refresh_interval: 30s
