# Monad Validator Monitor - runtime dependencies
#
# Pinned to the versions the suite runs against. Transitive packages are left
# floating on purpose: a lock generated on the dev interpreter (3.12) cannot be
# replayed on the image's 3.11 without risking unresolvable pins. Test-only
# packages live in requirements-dev.txt and never enter the image.
requests==2.34.2
pyyaml==6.0.3
ecdsa==0.19.2
aiohttp==3.14.3

# Monitoring Dashboard
fastapi==0.141.1
uvicorn[standard]==0.52.4
httpx==0.28.1
PyJWT==2.14.0
cachetools==7.1.8
